---
import Layout from "../layouts/Layout.astro";

const services = [
    {
        service: "Publishing",
        text: "We offer a variety of services regarding the publishing process. We will ensure that your work is copyrighted and registered. While we primarily focus on digital media, it may make sense for your project to print it physically as well. In that case, feel free to inquire about the availability!",
    },
    {
        service: "Editing",
        text: "Our team of professional editors specializes in working with queer authors and artists. we offer a range of editing services to fit your needs, from proofreading to copyediting and more. We will ensure that your project is polished and ready to share with the world.",
    },
    {
        service: "Collections",
        text: "Our collections showcase the work of queer authors and artists in the Pittsburgh region. We are always on the lookout for fresh, diverse voices. Our collections reflect our commitment to promoting underrepresented voices and celebrating the diversity of queer experiences.",
    },
];
---


    <section id="services" class="bg-secondary mt-8 py-6 pb-20 relative">
        <div class="divider-tilt-t">
            <svg data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
                <path d="M1200 120L0 16.48 0 0 1200 0 1200 120z" class="shape-fill"></path>
            </svg>
        </div>
        <div class="divider-tilt-b">
            <svg data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
                <path d="M1200 120L0 16.48 0 0 1200 0 1200 120z" class="shape-fill"></path>
            </svg>
        </div>
        <h2 class="h2">Services</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 px-3 gap-5 md:gap-10 max-w-7xl mx-auto">
        {
            services.map((service) => (
                <a href={"/services#"+service.service}><article class="md:max-w-[18rem] md:mx-auto">
                    <h3 class="font-sans font-bold text-2xl md:text-center">{service.service}</h3>
                    <p class="px-0 mt-4 mb-8">{service.text}</p>
                    </article></a>
            ))
        }</div>
    </section>

<style>

.divider-tilt-t {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    overflow: hidden;
    line-height: 0;
    fill:white;
    @apply hidden md:block;
}

.divider-tilt-t svg {
    position: relative;
    display: block;
    width: calc(212% + 1.3px);
    height: 137px;
}
.divider-tilt-b {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    overflow: hidden;
    line-height: 0;
    transform: rotate(180deg);
    fill:white;
}

.divider-tilt-b svg {
    position: relative;
    display: block;
    width: calc(212% + 1.3px);
    height: 137px;
}
</style>